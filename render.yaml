services:
  - type: web
    name: leadflow-crm
    env: python
    rootDir: leadflow
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0